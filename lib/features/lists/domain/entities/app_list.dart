class AppList {
  int? id;
  String? name;
  String? hexColor;
  String? icon;
  DateTime? createdAt;
  DateTime? updatedAt;

  AppList({
    this.id,
    this.name,
    this.hexColor,
    this.icon,
    this.createdAt,
    this.updatedAt,
  });
}
